var app = app || {};

app.Flight = Backbone.Model.extend({
  
  // Default values
  // Add event listeners
  // Initialize function
  // Anything else
  // Ways to display this piece of data
  // Validations

  urlRoot: "/flights",

});
