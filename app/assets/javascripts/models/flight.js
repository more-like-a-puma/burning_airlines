var app = app || {};

app.Flight = Backbone.Model.extend({
  // Default values
  // Add event listeners
  urlRoot: "/flights"
  // Initialize function
  // Anything else
    // Ways to display this piece of data
    // Validations
});
